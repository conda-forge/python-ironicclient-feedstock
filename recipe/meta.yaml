{% set name = "python-ironicclient" %}
{% set version = "2.7.0" %}
{% set sha256 = "b2b4dff87c39914dd940c3b35a2cde949503fef06272f321d3599b6e283e0fec" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr
    - keystoneauth1
    - oslo.utils
    - oslo.i18n
    - oslo.serialization
    - dogpile.cache
    - prettytable
    - appdirs
    - six

test:
  imports:
    - ironicclient
#  commands:
#    - ironic help
#    XXX:  cmdline seems broken upstream

about:
  home: https://docs.openstack.org/python-ironicclient/latest/
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Python Bindings to the OpenStack Ironic API
  dev_url: https://git.openstack.org/cgit/openstack/python-ironicclient

extra:
  recipe-maintainers:
    - brainstorm 
